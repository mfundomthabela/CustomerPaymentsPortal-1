# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/configuration-reference
version: 2.1 

# Define jobs for the workflow
jobs:
  # Job for saying hello
  say-hello:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run:
          name: "Say hello"
          command: "echo Hello, World!"

  # Job for SonarQube analysis
  sonarqube:
    working_directory: ~/build
    docker:
      - image: sonarsource/sonar-scanner-cli
    resource_class: small
    steps:
      - checkout
      - attach_workspace:
          at: ~/workspace
      - run:
          name: Run SonarQube Analysis
          command: |
            SONAR_BRANCH="${CIRCLE_BRANCH:-master}"
            echo "Sonar branch value is: $SONAR_BRANCH"
            echo "Sonar org value is: $SONAR_ORG"
            sonar-scanner \
              -Dsonar.projectKey="$SONAR_PROJECT_KEY" \
              -Dsonar.organization="$SONAR_ORG" \
              -Dsonar.host.url=https://sonarcloud.io \
              -Dsonar.exclusions="**/android/**,**/ios/**" \
              -Dsonar.login="$SONAR_TOKEN" \
              -Dsonar.branch.name="$SONAR_BRANCH" \
              -Dsonar.sources="."
              
# Job for Newman API testing
  newman-tests:
    docker:
      - image: postman/newman:latest
    steps:
      - checkout
      - run:
          name: Verify Postman Collection Exists
          command: |
            if [ ! -f tests/postman_collection.json ]; then
              echo "Error: Postman collection file is missing"; exit 1;
            fi
      - run:
          name: Run Newman Tests
          command: newman run customerpaymentsportal/tests/postman_collection.json

# Orchestrate jobs using workflows
workflows:
  full-pipeline:
    jobs:
      - say-hello
      - sonarqube
      - newman-tests
